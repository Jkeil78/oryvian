Einfache Medienverwaltung mit Discogs und Google Books Anbindung.

Installation via docker-compose.yml
Environment-Variablen:
APP_PORT (default 5000)
UPLOAD_PATH (default -./data/uploads)
DB_PATH (default -./data/instance)

Erster Start:
User: admin
PW: admin123
